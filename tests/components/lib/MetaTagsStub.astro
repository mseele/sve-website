---
// Stub MetaTags: drops all SEO/canonical computation that would otherwise
// call `Astro.site` (which the AstroContainer API leaves undefined by
// default). Rendered output is intentionally empty.
interface Props {}
const {} = Astro.props as Props
---